name: PR Comment
on: issue_comment

jobs:     
  debug:
    runs-on: ubuntu-latest    
    steps:
    - name: see payload
      run: echo "$PAYLOAD"
      env:
        PAYLOAD: ${{ toJSON(github.event) }}
  comment:
    runs-on: ubuntu-latest  
    steps:
      - run: echo "hello"
